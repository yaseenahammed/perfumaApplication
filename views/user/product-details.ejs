<%- include("../partials/user/header") %>

<main class="main">
  <% if (error) { %>
    <div class="alert alert-danger" style="text-align: center; padding: 10px; background: #ffdddd; color: #b00020; border: 1px solid #ffbbbb; margin: 20px auto; max-width: 600px; border-radius: 5px;">
      <%= error %>
    </div>
  <% } %>

  <section class="breadcrumb-section" style="background-image: url('/images/banner.jpg'); padding: 40px 0; background-size: cover; background-position: center;">
    <div class="container text-center text-white">
      <h2 class="breadcrumb-title" style="font-size: 2.2rem; font-weight: 700;"><%= product.name %></h2>
      <nav class="breadcrumb-text mt-2" style="font-size: 1rem;">
        <a href="/" class="text" style="text-decoration: none;">Home</a>
        <span class="mx-2">/</span>
        <a href="/shop" class="text" style="text-decoration: none;">shop</a>
        <span class="mx-2">/</span>
        <span><%= product.name %></span>
      </nav>
    </div>
  </section>
</main>

<!-- CSS for breadcrumb -->
<style>
  .breadcrumb-section {
    background-size: cover;
    background-position: center;
    color: gray;
  }

  .breadcrumb-section .breadcrumb-title {
    font-size: 2.2rem;
    font-weight: bold;
    margin-bottom: 10px;
  }

  .breadcrumb-section .breadcrumb-text a {
    color: gray;
    text-decoration: none;
  }

  .breadcrumb-section .breadcrumb-text span {
    color: gray;
  }
</style>

<main class="main">
  <section class="mt-50 mb-50">
    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <div class="product-detail accordion-detail">
            <div class="row mb-50">
              <div class="col-md-6 col-sm-12 col-xs-12">
                <div class="detail-gallery">
                  <span class="zoom-icon"><i class="fi-rs-search"></i></span>
                  <div class="main-product-image" style="position: relative;">
                    <figure class="border-radius-10">
                      <img
                        id="mainImage"
                        class="zoom-image"
                        src="<%= product.productImages && product.productImages.length > 0 ? product.productImages[0] : '/Uploads/product-images/default.jpg' %>"
                        alt="<%= product.name %>"
                        style="width: 100%; height: auto; max-height: 500px; object-fit: cover; border-radius: 10px;"
                      >
                      <div id="zoomLens" class="zoom-lens"></div>
                      <div id="zoomResult" class="zoom-result"></div>
                    </figure>
                  </div>
                  <div class="thumbnail-gallery">
                    <% if (product.productImages && product.productImages.length > 0) { %>
                      <% product.productImages.forEach((img, index) => { %>
                        <div class="thumbnail-item <%= index === 0 ? 'active' : '' %>">
                          <img
                            src="<%= img %>"
                            alt="<%= product.name %> Thumbnail <%= index + 1 %>"
                            onclick="changeMainImage('<%= img %>', this)"
                            style="width: 80px; height: 80px; object-fit: cover; border-radius: 5px;"
                          >
                        </div>
                      <% }) %>
                    <% } else { %>
                      <div class="thumbnail-item active">
                        <img
                          src="/Uploads/product-images/default.jpg"
                          alt="No Image"
                          onclick="changeMainImage('/Uploads/product-images/default.jpg', this)"
                          style="width: 80px; height: 80px; object-fit: cover; border-radius: 5px;"
                        >
                      </div>
                    <% } %>
                  </div>
                </div>
              </div>

              <div class="col-md-6 col-sm-12 col-xs-12">
                <div class="detail-info">
                  <h2 class="title-detail"><%= product.name %></h2>
                  <div class="product-detail-rating">
                    <div class="pro-details-brand">
                      <span>Brand: <a href="#"><%= product.brand?.name || 'Unknown' %></a></span>
                    </div>
                    <div class="product-rate-cover text-end">
                      <div class="product-rate d-inline-block">
                        <div class="product-rating" style="width:90%"></div>
                      </div>
                      <span class="font-small ml-5 text-muted">(25 reviews)</span>
                    </div>
                  </div>

                  <div class="clearfix product-price-cover">
                    <div class="product-price primary-color float-left">
                      <ins>
                        <span class="text-brand">₹<%= product.salePrice || product.regularPrice %></span>
                      </ins>
                      <% if (product.salePrice && product.salePrice < product.regularPrice) { %>
                        <ins>
                          <span class="old-price font-md ml-15">₹<%= product.regularPrice %></span>
                        </ins>
                        <span class="save-price font-md color3 ml-15">
                          <%= (product.regularPrice - product.salePrice) / product.regularPrice * 100 > 0 ? Math.round((product.regularPrice - product.salePrice) / product.regularPrice * 100) + '% Off' : 'Save ₹' + (product.regularPrice - product.salePrice) %>
                        </span>
                      <% } %>
                    </div>
                  </div>

                  <div class="bt-1 border-color-1 mt-15 mb-15"></div>
                  <div class="short-desc mb-30">
                    <p><%= product.description || 'No description available.' %></p>
                  </div>

                  <div class="product_sort_info font-xs mb-30">
                    <ul>
                      <li class="mb-10"><i class="fi-rs-crown mr-5"></i> 1 Year Brand Warranty</li>
                      <li class="mb-10"><i class="fi-rs-refresh mr-5"></i> 30 Day Return Policy</li>
                      <li><i class="fi-rs-credit-card mr-5"></i> Cash on Delivery available</li>
                    </ul>
                  </div>

                  <div class="bt-1 border-color-1 mt-30 mb-30"></div>

                  <div class="detail-extralink">
                    <div class="detail-qty border radius">
                      <a href="#" class="qty-down"><i class="fi-rs-angle-small-down"></i></a>
                      <span class="qty-val" id="quantity">1</span>
                      <a href="#" class="qty-up"><i class="fi-rs-angle-small-up"></i></a>
                    </div>
                    <div class="product-extra-link2">
                      <button type="button" class="button button-add-to-cart" id="addToCartBtn" data-product-id="<%= product._id %>" data-max-stock="<%= product.quantity %>" <%= product.quantity <= 0 || !product.isListed || product.isBlocked ? 'disabled' : '' %>>
                        Add to Cart
                      </button>
                      <a aria-label="Add To Wishlist" class="action-btn hover-up" onclick="addToWishlist('<%= product._id %>')"><i class="fi-rs-heart"></i></a>
                    </div>
                  </div>

                  <ul class="product-meta font-xs color-grey mt-50">
                    <li  class="mb-5">Stock Code: <%= product._id %></li>
                    <li class="mb-5">Tags:
                      <a href="#" rel="tag"><%= product.category?.name || 'No Category' %></a>
                    </li>
                    <li>Availability: <span class="in-stock text-success ml-5">
                      <%= product.status === 'available' ? 'In Stock' : 'Out of Stock' %> <%= product.quantity > 0 ? '(' + product.quantity + ' left)' : '' %>
                    </span></li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Similar Products Section -->
  <section class="mt-150 mb-50">
    <div class="container">
      <h3 class="section-title" style="font-size: 24px; font-weight: 600; margin-bottom: 30px; color: #333 !important; display: block !important; opacity: 1 !important; text-align: left;">Similar Products</h3>
      <div class="row">
        <% if (similarProducts && similarProducts.length > 0) { %>
          <% similarProducts.forEach(similarProduct => { %>
            <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12 mb-30">
              <div class="product-cart-wrap" style="border: 1px solid #e5e5e5; border-radius: 10px; padding: 15px; text-align: center; transition: transform 0.3s ease, box-shadow 0.3s ease;">
                <div class="product-img-action-wrap">
                  <div class="product-img">
                    <a href="/productDetails?id=<%= similarProduct._id %>">
                      <img
                        src="<%= similarProduct.productImages && similarProduct.productImages.length > 0 ? similarProduct.productImages[0] : '/Uploads/product-images/default.jpg' %>"
                        alt="<%= similarProduct.name %>"
                        style="width: 100%; height: 200px; object-fit: cover; border-radius: 10px; margin-bottom: 15px;"
                      >
                    </a>
                  </div>
                </div>
                <div class="product-content-wrap">
                  <h2 style="font-size: 16px; font-weight: 500; margin-bottom: 10px; color: #333; height: 40px; overflow: hidden; text-transform: uppercase;">
                    <a href="/products/<%= similarProduct._id %>" style="text-decoration: none; color: inherit;"><%= similarProduct.name %></a>
                  </h2>
                  <div class="product-price" style="margin-bottom: 15px;">
                    <span class="text-brand" style="font-size: 18px; font-weight: 600; color: #ff6b6b;">₹<%= similarProduct.salePrice || similarProduct.regularPrice %></span>
                    <% if (similarProduct.salePrice && similarProduct.salePrice < similarProduct.regularPrice) { %>
                      <span class="old-price" style="font-size: 14px; color: #999; text-decoration: line-through; margin-left: 10px;">₹<%= similarProduct.regularPrice %></span>
                    <% } %>
                  </div>
                  <div class="detail-extralink">
                    <div class="detail-qty border radius">
                      <a href="#" class="qty-down"><i class="fi-rs-angle-small-down"></i></a>
                      <span class="qty-val" id="similar-quantity-<%= similarProduct._id %>">1</span>
                      <a href="#" class="qty-up"><i class="fi-rs-angle-small-up"></i></a>
                    </div>
                    <div class="product-extra-link2">
                      <button type="button" class="button button-add-to-cart" id="addToCartBtn-<%= similarProduct._id %>" data-product-id="<%= similarProduct._id %>" data-max-stock="<%= similarProduct.quantity %>" <%= similarProduct.quantity <= 0 || !similarProduct.isListed || similarProduct.isBlocked ? 'disabled' : '' %>>
                        Add to Cart
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <style>
                .product-cart-wrap:hover {
                  transform: translateY(-5px);
                  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
                }
                .product-action-1 a:hover {
                  background: #ff6b6b !important;
                  color: #fff !important;
                }
                .product-action-1 a:hover i {
                  color: #fff !important;
                }
              </style>
            </div>
          <% }) %>
        <% } else { %>
          <div class="col-12">
            <p style="font-size: 16px; color: #666; text-align: center;">No similar products found in this category.</p>
          </div>
        <% } %>
      </div>
    </div>
  </section>
</main>

<!-- Updated Custom CSS -->
<style>
  .product-detail {
    background: #fff;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    margin-top: 30px;
  }

  .detail-gallery {
    position: relative;
    max-width: 500px;
    margin: 0 auto;
  }

  .main-product-image img {
    width: 100%;
    height: auto;
    max-height: 500px;
    object-fit: cover;
    border-radius: 10px;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
    transition: transform 0.4s ease, box-shadow 0.4s ease;
  }

  .main-product-image img:hover {
    transform: scale(1.02);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
  }

  .zoom-lens {
    position: absolute;
    width: 100px;
    height: 100px;
    border: 2px solid #d3a07b;
    background: rgba(0, 0, 0, 0.2);
    pointer-events: none;
    display: none;
    border-radius: 50%;
    z-index: 100;
  }

  .zoom-result {
    position: absolute;
    width: 300px;
    height: 300px;
    border: 2px solid #d3a07b;
    background-repeat: no-repeat;
    display: none;
    z-index: 100;
    top: 0;
    left: 520px;
    border-radius: 10px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    background-color: #fff;
  }

  .thumbnail-gallery {
    display: flex;
    gap: 10px;
    margin-top: 15px;
    overflow-x: auto;
    padding: 5px 0;
  }

  .thumbnail-item {
    flex: 0 0 auto;
    width: 80px;
    height: 80px;
    cursor: pointer;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    transition: border-color 0.3s ease, transform 0.3s ease;
  }

  .thumbnail-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 6px;
  }

  .thumbnail-item.active,
  .thumbnail-item:hover {
    border-color: #d3a07b;
    transform: scale(1.05);
  }

  .zoom-icon {
    position: absolute;
    top: 10px;
    right: 10px;
    z-index: 10;
    color: #d3a07b;
    font-size: 22px;
    cursor: pointer;
    background: rgba(255, 255, 255, 0.8);
    padding: 5px;
    border-radius: 50%;
    transition: transform 0.3s ease;
  }

  .zoom-icon:hover {
    transform: scale(1.2);
  }

  .detail-info {
    padding-left: 30px;
  }

  .title-detail {
    font-size: 2rem;
    color: #2c2c2c;
    margin-bottom: 15px;
    font-weight: 600;
  }

  .product-detail-rating .pro-details-brand {
    margin-bottom: 10px;
  }

  .product-detail-rating .pro-details-brand span a {
    color: #007bff;
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .product-detail-rating .pro-details-brand span a:hover {
    color: #0056b3;
  }

  .product-rate-cover {
    margin-bottom: 10px;
  }

  .product-price {
    font-size: 1.8rem;
    color: #d3a07b;
    margin-bottom: 20px;
    font-weight: 500;
  }

  .old-price {
    color: #888;
    text-decoration: line-through;
    margin-left: 10px;
    font-size: 1.2rem;
  }

  .save-price {
    color: #28a745;
    font-weight: bold;
    margin-left: 10px;
  }

  .short-desc {
    color: #666;
    margin-bottom: 20px;
    line-height: 1.5;
  }

  .product_sort_info ul {
    list-style: none;
    color: #555;
  }

  .product_sort_info li {
    margin-bottom: 10px;
    display: flex;
    align-items: center;
  }

  .product_sort_info i {
    color: #d3a07b;
    margin-right: 8px;
  }

  .detail-qty {
    display: inline-flex;
    align-items: center;
    border: 1px solid #ddd;
    border-radius: 5px;
    overflow: hidden;
    margin-right: 15px;
  }

  .qty-down, .qty-up {
    padding: 8px 12px;
    background: #f5f5f5;
    cursor: pointer;
    transition: background 0.3s ease;
  }

  .qty-down:hover, .qty-up:hover {
    background: #e0e0e0;
  }

  .qty-val {
    padding: 8px 12px;
    min-width: 40px;
    text-align: center;
    border-left: 1px solid #ddd;
    border-right: 1px solid #ddd;
  }

  .button-add-to-cart {
    background-color: #d3a07b;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
  }

  .button-add-to-cart:hover {
    background-color: #b3886a;
    transform: translateY(-2px);
  }

  .button-add-to-cart:disabled {
    background-color: #ccc;
    cursor: not-allowed;
  }

  .action-btn {
    display: inline-block;
    padding: 10px;
    background: #f5f5f5;
    border-radius: 50%;
    margin-left: 10px;
    transition: background 0.3s ease, transform 0.3s ease;
  }

  .action-btn:hover {
    background: #d3a07b;
    transform: scale(1.1);
  }

  .action-btn i {
    color: #333;
    font-size: 16px;
  }

  .action-btn:hover i {
    color: #fff;
  }

  .product-meta {
    list-style: none;
    color: #666;
    margin-top: 20px;
  }

  .product-meta li {
    margin-bottom: 8px;
  }

  .in-stock {
    color: #28a745;
  }

  .button-add-to-cart.loading {
    position: relative;
    pointer-events: none;
  }
  .button-add-to-cart.loading:after {
    content: '';
    position: absolute;
    width: 16px;
    height: 16px;
    border: 2px solid #fff;
    border-top-color: transparent;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }
  @keyframes spin {
    to { transform: translate(-50%, -50%) rotate(360deg); }
  }

  @media (max-width: 768px) {
    .detail-gallery {
      max-width: 100%;
    }
    .zoom-result {
      display: none;
    }
    .zoom-lens {
      display: none;
    }
    .main-product-image img {
      max-height: 350px;
    }
    .detail-info {
      padding-left: 0;
      margin-top: 20px;
    }
    .col-md-6 {
      width: 100%;
    }
  }
</style>

<!-- Updated JavaScript for Zoom, Touch, and Add to Cart -->
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const zoomContainer = document.querySelector('.main-product-image');
    const zoomImage = document.getElementById('mainImage');
    const zoomLens = document.getElementById('zoomLens');
    const zoomResult = document.getElementById('zoomResult');

    let zoomLevel = 2.5;
    let isTouching = false;

   
    function updateZoom(e, clientX, clientY) {
      const rect = zoomImage.getBoundingClientRect();
      const x = clientX - rect.left;
      const y = clientY - rect.top;

      const lensWidth = zoomLens.offsetWidth;
      const lensHeight = zoomLens.offsetHeight;
      let lensX = x - lensWidth / 2;
      let lensY = y - lensHeight / 2;

      lensX = Math.max(0, Math.min(lensX, rect.width - lensWidth));
      lensY = Math.max(0, Math.min(lensY, rect.height - lensHeight));

      zoomLens.style.left = `${lensX}px`;
      zoomLens.style.top = `${lensY}px`;

      zoomResult.style.backgroundImage = `url(${zoomImage.src})`;
      zoomResult.style.backgroundSize = `${rect.width * zoomLevel}px ${rect.height * zoomLevel}px`;
      zoomResult.style.backgroundPosition = `-${lensX * zoomLevel}px -${lensY * zoomLevel}px`;
    }

    zoomContainer.addEventListener('mouseenter', () => {
      if (window.innerWidth > 768) {
        zoomLens.style.display = 'block';
        zoomResult.style.display = 'block';
      }
    });

    zoomContainer.addEventListener('mouseleave', () => {
      zoomLens.style.display = 'none';
      zoomResult.style.display = 'none';
    });

    zoomContainer.addEventListener('mousemove', (e) => {
      if (window.innerWidth > 768) updateZoom(e, e.clientX, e.clientY);
    });

   
    zoomContainer.addEventListener('touchstart', (e) => {
      e.preventDefault();
      if (window.innerWidth <= 768) {
        isTouching = true;
        zoomLens.style.display = 'block';
        zoomResult.style.display = 'block';
        const touch = e.touches[0];
        updateZoom(e, touch.clientX, touch.clientY);
      }
    });

    zoomContainer.addEventListener('touchmove', (e) => {
      if (isTouching && window.innerWidth <= 768) {
        e.preventDefault();
        const touch = e.touches[0];
        updateZoom(e, touch.clientX, touch.clientY);
      }
    });

    zoomContainer.addEventListener('touchend', () => {
      isTouching = false;
      zoomLens.style.display = 'none';
      zoomResult.style.display = 'none';
    });

    window.changeMainImage = function (imageSrc, element) {
      zoomImage.src = imageSrc;
      zoomResult.style.backgroundImage = `url(${imageSrc})`;
      document.querySelectorAll('.thumbnail-item').forEach((thumb) => {
        thumb.classList.remove('active');
        thumb.classList.remove('border-blue-500');
        thumb.classList.add('border-gray-200');
      });
      element.parentElement.classList.add('active');
      element.parentElement.classList.add('border-blue-500');
      element.parentElement.classList.remove('border-gray-200');
    };

    const qtyDown = document.querySelector('.detail-qty .qty-down');
    const qtyUp = document.querySelector('.detail-qty .qty-up');
    const qtyVal = document.getElementById('quantity');
    const maxStock = parseInt(document.querySelector('#addToCartBtn')?.getAttribute('data-max-stock')) || 0;

    qtyDown.addEventListener('click', (e) => {
      e.preventDefault();
      let value = parseInt(qtyVal.textContent);
      if (value > 1) qtyVal.textContent = value - 1;
    });

    qtyUp.addEventListener('click', (e) => {
      e.preventDefault();
      let value = parseInt(qtyVal.textContent);
      if (value < maxStock) qtyVal.textContent = value + 1;
    });



    

  
    const addToCartBtn = document.getElementById('addToCartBtn');
    if (addToCartBtn) {
      addToCartBtn.addEventListener('click', async () => {
        const productId = addToCartBtn.getAttribute('data-product-id');
        const quantity = parseInt(qtyVal.textContent);

        addToCartBtn.classList.add('loading');
        try {
          const response = await fetch(`/add-to-cart/${productId}`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ quantity }),
          });
          const data = await response.json();
          if (data.success) {
            alert(data.message || 'Product added to cart successfully!');
            window.location.href = '/cart';
          } else {
            alert(data.error || 'Failed to add to cart');
          }
        } catch (error) {
          alert('An error occurred. Please try again.');
        } finally {
          addToCartBtn.classList.remove('loading');
        }
      });
    }

 
    document.querySelectorAll('[id^="addToCartBtn-"]').forEach(button => {
      const productId = button.getAttribute('data-product-id');
      const qtyVal = document.getElementById(`similar-quantity-${productId}`);
      const qtyDown = button.closest('.product-cart-wrap').querySelector('.qty-down');
      const qtyUp = button.closest('.product-cart-wrap').querySelector('.qty-up');
      const maxStock = parseInt(button.getAttribute('data-max-stock')) || 0;

      qtyDown.addEventListener('click', (e) => {
        e.preventDefault();
        let value = parseInt(qtyVal.textContent);
        if (value > 1) qtyVal.textContent = value - 1;
      });

      qtyUp.addEventListener('click', (e) => {
        e.preventDefault();
        let value = parseInt(qtyVal.textContent);
        if (value < maxStock) qtyVal.textContent = value + 1;
      });

      button.addEventListener('click', async () => {
        const quantity = parseInt(qtyVal.textContent);

        button.classList.add('loading');
        try {
          const response = await fetch(`/add-to-cart/${productId}`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ quantity }),
          });
          const data = await response.json();
          if (data.success) {
            alert(data.message || 'Product added to cart successfully!');
            window.location.href = '/cart';
          } else {
            alert(data.error || 'Failed to add to cart');
          }
        } catch (error) {
          alert('An error occurred. Please try again.');
        } finally {
          button.classList.remove('loading');
        }
      });
    });

    window.addToWishlist = (productId) => {
      console.log('Add to wishlist:', productId);
    
    };
  });
</script>

<%- include("../partials/user/footer") %>